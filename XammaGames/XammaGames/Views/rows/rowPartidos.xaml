<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
		xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
		x:Class="XammaGames.rowPartidos"
		>
	<ContentView.Content>
		<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="5" />
					<ColumnDefinition Width="*" />
					<ColumnDefinition Width="auto" />
					<ColumnDefinition Width="5" />
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition Height="*"/>
					<RowDefinition Height="*"/>

				</Grid.RowDefinitions>
			
				<Frame HasShadow="true"  OutlineColor="Black" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="4" Grid.RowSpan="2">
				</Frame>
			
			<Label Grid.Column="1" Grid.Row="0" Text="{Binding Usuario1}" Style="{DynamicResource  ListItemTextStyle}" TextColor="#B7BFBB" VerticalTextAlignment="Center"   HorizontalOptions="Start" />
			<Label Grid.Column="1" Grid.Row="0" Text="{Binding Score1}" Style="{DynamicResource  ListItemTextStyle}" TextColor="#B7BFBB" VerticalTextAlignment="Center"   HorizontalOptions="End" />
			
			<Label Grid.Column="1" Grid.Row="1" Text="{Binding Usuario2}" Style="{DynamicResource  ListItemTextStyle}" TextColor="#B7BFBB" VerticalTextAlignment="Center"   HorizontalOptions="Start" />
			<Label Grid.Column="1" Grid.Row="1" Text="{Binding Score2}" Style="{DynamicResource  ListItemTextStyle}" TextColor="#B7BFBB" VerticalTextAlignment="Center"   HorizontalOptions="End" />
			
			<Label Grid.Column="2" Grid.Row="0" Grid.RowSpan="2" Text="{Binding}" Style="{DynamicResource  ListItemTextStyle}" TextColor="#B7BFBB" VerticalTextAlignment="Center"   HorizontalOptions="Start" />

			
		</Grid>
	</ContentView.Content>
</ContentView>
